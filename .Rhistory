fold = integer(0),
submodel = integer(0),
class_pos = character(0),
class_neg = character(0),
n_samples = integer(0),
auroc = numeric(0),
auprc = numeric(0),
stringsAsFactors = FALSE
)
for (k in seq_len(n_submodels)) {
classes_k <- get_ovo_classes_from_confmat(
confusion_list[[k]]
)
class_pos <- classes_k[1]
class_neg <- classes_k[2]
idx <- which(
y_test_fold %in% c(class_pos, class_neg)
)
y_true <- ifelse(
y_test_fold[idx] == class_pos,
1,
0
)
y_score <- scores_predictions[[k]][idx]
if (length(y_score) != length(y_true)) {
stop(
paste0(
"Length mismatch in ", dataset_name,
" fold ", fold_id,
", submodel ", k,
": length(y_score) = ", length(y_score),
", length(y_true) = ", length(y_true)
)
)
}
met <- compute_binary_auc_metrics(
y_true = y_true,
y_score = y_score
)
results <- rbind(
results,
data.frame(
dataset = dataset_name,
fold = fold_id,
submodel = k,
class_pos = class_pos,
class_neg = class_neg,
n_samples = length(idx),
auroc = met$auroc,
auprc = met$auprc,
stringsAsFactors = FALSE
)
)
}
return(results)
}
# =========================================================
# 4. Compute AUROC and AUPRC across all folds
# =========================================================
compute_all_folds_binary_metrics <- function(res_object, dataset_name) {
n_folds <- length(res_object$crossVal$best_fold_test)
all_results <- data.frame()
for (fold_id in seq_len(n_folds)) {
fold_results <- compute_fold_binary_metrics(
res_object = res_object,
fold_id = fold_id,
dataset_name = dataset_name
)
all_results <- rbind(all_results, fold_results)
}
return(all_results)
}
# =========================================================
# 5. Summarize AUROC and AUPRC results
# =========================================================
summarize_binary_metrics <- function(all_results) {
submodel_summary <- all_results %>%
group_by(dataset, submodel, class_pos, class_neg) %>%
summarise(
mean_auroc = mean(auroc, na.rm = TRUE),
mean_auprc = mean(auprc, na.rm = TRUE),
.groups = "drop"
)
global_summary <- all_results %>%
group_by(dataset) %>%
summarise(
mean_auroc = mean(auroc, na.rm = TRUE),
sd_auroc = sd(auroc, na.rm = TRUE),
mean_auprc = mean(auprc, na.rm = TRUE),
sd_auprc = sd(auprc, na.rm = TRUE),
n_results = n(),
.groups = "drop"
)
return(list(
submodel_summary = submodel_summary,
global_summary = global_summary
))
}
# =========================================================
# 6. Run analysis for the four Predomics datasets
# =========================================================
all_metrics_crc <- compute_all_folds_binary_metrics(
res_object = res_clf_mc_CRC_2026,
dataset_name = "CRC"
)
all_metrics_t2d <- compute_all_folds_binary_metrics(
res_object = res_clf_mc_T2D_2026,
dataset_name = "T2D"
)
all_metrics_balance <- compute_all_folds_binary_metrics(
res_object = res_clf_mc_balance_2026,
dataset_name = "Enterotype Balanced"
)
all_metrics_imbalance <- compute_all_folds_binary_metrics(
res_object = res_clf_mc_imbalance_2026,
dataset_name = "Enterotype Imbalanced"
)
# =========================================================
# 7. Merge all results
# =========================================================
all_metrics_predomics <- bind_rows(
all_metrics_crc,
all_metrics_t2d,
all_metrics_balance,
all_metrics_imbalance
)
# =========================================================
# 8. Summarize all results
# =========================================================
summary_metrics_predomics <- summarize_binary_metrics(
all_metrics_predomics
)
cat("\n=== Mean AUROC/AUPRC by OvO submodel ===\n")
print(summary_metrics_predomics$submodel_summary)
cat("\n=== Global mean AUROC/AUPRC by dataset ===\n")
print(summary_metrics_predomics$global_summary)
# =========================================================
# Final article-ready table
# =========================================================
final_auc_table_predomics <- summary_metrics_predomics$global_summary %>%
mutate(
AUROC = paste0(round(mean_auroc, 3), " ± ", round(sd_auroc, 3)),
AUPRC = paste0(round(mean_auprc, 3), " ± ", round(sd_auprc, 3))
) %>%
select(dataset, AUROC, AUPRC)
print(final_auc_table_predomics)
# =========================================================
# AUROC and AUPRC computation for Predomics OvO submodels
# across 10-fold cross-validation for four datasets
# =========================================================
library(pROC)
library(PRROC)
# =========================================================
# 1. Compute AUROC and AUPRC for one binary classifier
# =========================================================
compute_binary_auc_metrics <- function(y_true, y_score) {
keep_idx <- which(!is.na(y_true) & !is.na(y_score))
y_true <- y_true[keep_idx]
y_score <- y_score[keep_idx]
if (length(unique(y_true)) < 2) {
return(list(
auroc = NA_real_,
auprc = NA_real_
))
}
roc_obj <- pROC::roc(
response = y_true,
predictor = y_score,
quiet = TRUE
)
auroc_value <- as.numeric(pROC::auc(roc_obj))
scores_pos <- y_score[y_true == 1]
scores_neg <- y_score[y_true == 0]
pr_obj <- PRROC::pr.curve(
scores.class0 = scores_pos,
scores.class1 = scores_neg,
curve = FALSE
)
auprc_value <- pr_obj$auc.integral
return(list(
auroc = auroc_value,
auprc = auprc_value
))
}
# =========================================================
# 2. Extract the two classes of one OvO submodel
# =========================================================
get_ovo_classes_from_confmat <- function(confmat) {
return(rownames(confmat))
}
# =========================================================
# 3. Compute AUROC and AUPRC for one fold
# =========================================================
compute_fold_binary_metrics <- function(res_object, fold_id, dataset_name) {
fold_test <- res_object$crossVal$best_fold_test[[fold_id]]
fold_xy <- res_object$crossVal$best_fold_xy[[fold_id]]
confusion_list <- fold_test$confusionMatrix_
scores_predictions <- fold_test$scores_predictions
y_test_fold <- fold_xy$y_test
n_submodels <- length(confusion_list)
results <- data.frame(
dataset = character(0),
fold = integer(0),
submodel = integer(0),
class_pos = character(0),
class_neg = character(0),
n_samples = integer(0),
auroc = numeric(0),
auprc = numeric(0),
stringsAsFactors = FALSE
)
for (k in seq_len(n_submodels)) {
classes_k <- get_ovo_classes_from_confmat(
confusion_list[[k]]
)
class_pos <- classes_k[1]
class_neg <- classes_k[2]
idx <- which(
y_test_fold %in% c(class_pos, class_neg)
)
y_true <- ifelse(
y_test_fold[idx] == class_pos,
1,
0
)
y_score <- scores_predictions[[k]][idx]
if (length(y_score) != length(y_true)) {
stop(
paste0(
"Length mismatch in ", dataset_name,
" fold ", fold_id,
", submodel ", k,
": length(y_score) = ", length(y_score),
", length(y_true) = ", length(y_true)
)
)
}
met <- compute_binary_auc_metrics(
y_true = y_true,
y_score = y_score
)
results <- rbind(
results,
data.frame(
dataset = dataset_name,
fold = fold_id,
submodel = k,
class_pos = class_pos,
class_neg = class_neg,
n_samples = length(idx),
auroc = met$auroc,
auprc = met$auprc,
stringsAsFactors = FALSE
)
)
}
return(results)
}
# =========================================================
# 4. Compute AUROC and AUPRC across all folds
# =========================================================
compute_all_folds_binary_metrics <- function(res_object, dataset_name) {
n_folds <- length(res_object$crossVal$best_fold_test)
all_results <- data.frame()
for (fold_id in seq_len(n_folds)) {
fold_results <- compute_fold_binary_metrics(
res_object = res_object,
fold_id = fold_id,
dataset_name = dataset_name
)
all_results <- rbind(all_results, fold_results)
}
return(all_results)
}
# =========================================================
# 5. Summarize AUROC and AUPRC results
# =========================================================
summarize_binary_metrics <- function(all_results) {
submodel_summary <- all_results %>%
group_by(dataset, submodel, class_pos, class_neg) %>%
summarise(
mean_auroc = mean(auroc, na.rm = TRUE),
mean_auprc = mean(auprc, na.rm = TRUE),
.groups = "drop"
)
global_summary <- all_results %>%
group_by(dataset) %>%
summarise(
mean_auroc = mean(auroc, na.rm = TRUE),
sd_auroc = sd(auroc, na.rm = TRUE),
mean_auprc = mean(auprc, na.rm = TRUE),
sd_auprc = sd(auprc, na.rm = TRUE),
n_results = n(),
.groups = "drop"
)
return(list(
submodel_summary = submodel_summary,
global_summary = global_summary
))
}
# =========================================================
# 6. Run analysis for the four Predomics datasets
# =========================================================
all_metrics_crc <- compute_all_folds_binary_metrics(
res_object = res_clf_mc_CRC_2026,
dataset_name = "CRC"
)
all_metrics_t2d <- compute_all_folds_binary_metrics(
res_object = res_clf_mc_T2D_2026,
dataset_name = "T2D"
)
all_metrics_balance <- compute_all_folds_binary_metrics(
res_object = res_clf_mc_balance_2026,
dataset_name = "Enterotype Balanced"
)
all_metrics_imbalance <- compute_all_folds_binary_metrics(
res_object = res_clf_mc_imbalance_2026,
dataset_name = "Enterotype Imbalanced"
)
# =========================================================
# 7. Merge all results
# =========================================================
all_metrics_predomics <- bind_rows(
all_metrics_crc,
all_metrics_t2d,
all_metrics_balance,
all_metrics_imbalance
)
# =========================================================
# 8. Summarize all results
# =========================================================
summary_metrics_predomics <- summarize_binary_metrics(
all_metrics_predomics
)
cat("\n=== Mean AUROC/AUPRC by OvO submodel ===\n")
print(summary_metrics_predomics$submodel_summary)
cat("\n=== Global mean AUROC/AUPRC by dataset ===\n")
print(summary_metrics_predomics$global_summary)
# =========================================================
# Final article-ready table
# =========================================================
final_auc_table_predomics <- summary_metrics_predomics$global_summary %>%
mutate(
AUROC = paste0(round(mean_auroc, 3), " ± ", round(sd_auroc, 3)),
AUPRC = paste0(round(mean_auprc, 3), " ± ", round(sd_auprc, 3))
) %>%
select(dataset, AUROC, AUPRC)
print(final_auc_table_predomics)
# =========================================================
# =======================================================
# 1. Prepare the global dataframe
# =========================================================
df_global <- Analysis_Dataset_Complet_Sota_vs_Predo
# Convert Accuracy column into numeric value for sorting
# Example: "0.594 ± 0.076" becomes 0.594
df_global <- df_global %>%
mutate(
Accuracy_mean = as.numeric(str_extract(Accuracy, "^[0-9.]+"))
)
# =========================================================
# 2. Display raw performances sorted by test accuracy
# =========================================================
df_sorted <- df_global %>%
arrange(Dataset, desc(Accuracy_mean))
print(df_sorted)
# =========================================================
# 3. Display best performances by dataset
# =========================================================
df_best_by_dataset <- df_global %>%
group_by(Dataset) %>%
arrange(desc(Accuracy_mean), .by_group = TRUE) %>%
slice(1) %>%
ungroup()
print(df_best_by_dataset)
# =========================================================
# 4. Split dataframe by dataset
# =========================================================
df_crc <- df_global %>%
filter(Dataset == "CRC") %>%
arrange(desc(Accuracy_mean))
df_t2d <- df_global %>%
filter(Dataset == "T2D") %>%
arrange(desc(Accuracy_mean))
df_balance <- df_global %>%
filter(Dataset == "Enterotype Balanced") %>%
arrange(desc(Accuracy_mean))
df_imbalance <- df_global %>%
filter(Dataset == "Enterotype Imbalanced") %>%
arrange(desc(Accuracy_mean))
# =========================================================
# 5. Save the four CSV files
# =========================================================
write_csv(df_crc, "CRC_performances_sorted_by_test_accuracy.csv")
write_csv(df_t2d, "T2D_performances_sorted_by_test_accuracy.csv")
write_csv(df_balance, "Enterotype_Balanced_performances_sorted_by_test_accuracy.csv")
write_csv(df_imbalance, "Enterotype_Imbalanced_performances_sorted_by_test_accuracy.csv")
# =========================================================
# =========================================================
# 1. Keep ONLY TEST performances
# =========================================================
df_global <- Analysis_Dataset_Complet_Sota_vs_Predo %>%
filter(
grepl("test", Type, ignore.case = TRUE)
)
# =========================================================
# =========================================================
# 1. Keep ONLY TEST performances
library(readr)
library(stringr)
# =========================================================
df_global <- Analysis_Dataset_Complet_Sota_vs_Predo %>%
filter(
grepl("test", Type, ignore.case = TRUE)
)
Analysis_Dataset_Complet_Sota_vs_Predo$Set
# =========================================================
# =========================================================
# 1. Keep ONLY TEST performances
library(readr)
library(stringr)
# =========================================================
df_global <- Analysis_Dataset_Complet_Sota_vs_Predo %>%
filter(
grepl("Test", Type, ignore.case = TRUE)
)
colnames(Analysis_Dataset_Complet_Sota_vs_Predo)
Analysis_Dataset_Complet_Sota_vs_Predo$Set
colnames(Analysis_Dataset_Complet_Sota_vs_Predo)
unique(Analysis_Dataset_Complet_Sota_vs_Predo$Set)
# =========================================================
# Libraries
library(readr)
library(stringr)
# =========================================================
# 1. Keep ONLY TEST performances
# =========================================================
df_global <- Analysis_Dataset_Complet_Sota_vs_Predo %>%
filter(Set == "Test")
# =========================================================
# 2. Extract numeric test accuracy
# Example:
# "0.594 ± 0.076" -> 0.594
# =========================================================
df_global <- df_global %>%
mutate(
Accuracy_mean = as.numeric(
str_extract(Accuracy, "^[0-9.]+")
)
)
# =========================================================
# 3. Sort performances by TEST accuracy
# =========================================================
df_sorted <- df_global %>%
arrange(
Dataset,
desc(Accuracy_mean)
)
print(df_sorted)
# =========================================================
# 4. Best TEST performance per dataset
# =========================================================
df_best_by_dataset <- df_sorted %>%
group_by(Dataset) %>%
slice(1) %>%
ungroup()
print(df_best_by_dataset)
# =========================================================
# 5. Split datasets
# =========================================================
df_crc <- df_sorted %>%
filter(Dataset == "CRC")
df_t2d <- df_sorted %>%
filter(Dataset == "T2D")
df_balance <- df_sorted %>%
filter(Dataset == "Enterotype Balanced")
df_imbalance <- df_sorted %>%
filter(Dataset == "Enterotype Imbalanced")
# =========================================================
# 6. Save CSV files
# =========================================================
write_csv(
df_crc,
"CRC_TEST_performances_sorted_by_accuracy.csv"
)
write_csv(
df_t2d,
"T2D_TEST_performances_sorted_by_accuracy.csv"
)
write_csv(
df_balance,
"Enterotype_Balanced_TEST_performances_sorted_by_accuracy.csv"
)
write_csv(
df_imbalance,
"Enterotype_Imbalanced_TEST_performances_sorted_by_accuracy.csv"
)
# =========================================================
# 7. Optional summary table
# =========================================================
df_best_summary <- df_best_by_dataset %>%
select(
Dataset,
Methods,
Approach,
Binarisation,
Constraint_factor,
Accuracy,
Precision,
Recall,
F1
)
print(df_best_summary)
