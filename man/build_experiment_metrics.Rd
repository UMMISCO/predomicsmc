% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_all_experiments_metrics_df.R
\name{build_experiment_metrics}
\alias{build_experiment_metrics}
\title{Build cross-validation experiment metrics dataframe}
\usage{
build_experiment_metrics(
  res_obj,
  k_name,
  total_instances_train,
  total_instances_test,
  method_name,
  k_value,
  constraint_factor,
  dataset_name,
  approch_name,
  binarisation,
  num_folds = 10
)
}
\arguments{
\item{res_obj}{A Predomics result object containing cross-validation scores.
It must include the following elements:
\code{res_obj$crossVal$scores$empirical.acc},
\code{res_obj$crossVal$scores$empirical.rec},
\code{res_obj$crossVal$scores$empirical.prc},
\code{res_obj$crossVal$scores$empirical.f1s},
\code{res_obj$crossVal$scores$generalization.acc},
\code{res_obj$crossVal$scores$generalization.rec},
\code{res_obj$crossVal$scores$generalization.prc},
and \code{res_obj$crossVal$scores$generalization.f1s}.}

\item{k_name}{Character string indicating the selected sparsity level.
For example, \code{"k_9"} indicates the best model using 9 selected variables
or the selected sparsity configuration.}

\item{total_instances_train}{Numeric value indicating the total number of
training instances used during cross-validation.}

\item{total_instances_test}{Numeric value indicating the total number of
test instances used during cross-validation.}

\item{method_name}{Character string indicating the aggregation or
classification method used. Examples include
\code{"Voting_with_Tie_Breaking"}, \code{"Random_Forest"}, or \code{"SVM"}.}

\item{k_value}{Character or numeric value indicating the total number of
variables/features in the original dataset. For example, \code{"3385"}.}

\item{constraint_factor}{Character string indicating the constraint level
used during variable selection. Examples include \code{"Semi_Constrained"},
\code{"Fully_Constrained"}, or \code{"Unconstrained"}.}

\item{dataset_name}{Character string indicating the dataset name.
Examples include \code{"Balanced Enterotype"}, \code{"Imbalanced Enterotype"},
\code{"CRC"}, or \code{"T2D"}.}

\item{approch_name}{Character string indicating the classification approach.
Examples include \code{"Terbeam Predomics"}, \code{"SVM"}, or \code{"Random Forest"}.
The name \code{approch_name} is intentionally kept to match the existing code.}

\item{binarisation}{Character string indicating the binarisation strategy.
Examples include \code{"OVO"} for One-vs-One and \code{"OVA"} for One-vs-All.}

\item{num_folds}{Numeric value indicating the number of cross-validation
folds. Default is \code{10}.}
}
\value{
A dataframe containing training and test metrics for each fold.
The dataframe includes the following columns:
\code{Fold}, \code{Accuracy}, \code{Recall}, \code{Precision}, \code{F1}, \code{TP}, \code{FP}, \code{FN},
\code{ErrorRate}, \code{Methods}, \code{K}, \code{Constraint_factor}, \code{Dataset},
\code{Approach}, \code{Set}, and \code{Binarisation}.
}
\description{
This function extracts empirical (training) and generalization (test)
performance metrics from a Predomics cross-validation result object and
converts them into a tidy dataframe. The returned dataframe contains one row
per fold and per data split (\code{Train} and \code{Test}), including accuracy, recall,
precision, F1-score, approximate TP/FP/FN values, error rate, and experiment
metadata.
}
\details{
The values of \code{TP}, \code{FP}, and \code{FN} are approximate values derived from
recall, precision, and the total number of instances. They should therefore
be interpreted as reconstructed indicators rather than exact confusion
matrix counts.

The function performs a security check to ensure that all metric vectors
contain the expected number of folds. If one metric has an incorrect length,
the function stops and returns an explicit error message.
}
\examples{
experiment_df <- build_experiment_metrics(
  res_obj = res_clf_mc_imbalance_2026,
  k_name = "k_9",
  total_instances_train = 486,
  total_instances_test = 54,
  method_name = "Voting_with_Tie_Breaking",
  k_value = "3385",
  constraint_factor = "Semi_Constrained",
  dataset_name = "Imbalanced Enterotype",
  approch_name = "Terbeam Predomics",
  binarisation = "OVO",
  num_folds = 10
)

}
